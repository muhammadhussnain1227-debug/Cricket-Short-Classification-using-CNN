version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: cricket
      POSTGRES_PASSWORD: cricket123
      POSTGRES_DB: cricketdb
    ports:
      - "5432:5432"